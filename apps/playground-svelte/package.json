{
  "name": "dotimation-playground-svelte",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "svelte-check --tsconfig ./tsconfig.json && vite build"
  },
  "dependencies": {
    "@dotimation/svelte": "workspace:*",
    "svelte": "^5.29.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "vite": "^8.0.10",
    "svelte-check": "^4.0.0"
  }
}
